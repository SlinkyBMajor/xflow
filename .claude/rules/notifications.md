## Notifications

### Toasts
Use for transient feedback on completed actions (merge success, cleanup done, errors). Auto-dismiss ~4s.
Import `toast` from `"sonner"` directly.

### Inline messages
Use when content is copyable (PR URLs, error details, branch names) or when user needs to act on it (conflict file lists).

Both can be used together.
